[project]
name = "ccd2smiles"
version = "0.1.0"
dependencies = [
    "requests",
]

[project.scripts]
ccd2smiles = "ccd2smiles.ccd2smiles:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
